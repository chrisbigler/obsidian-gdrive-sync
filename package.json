{
  "name": "obsidian-gdrive-sync",
  "version": "1.0.0",
  "description": "Sync Google Drive meeting docs to Obsidian as markdown",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "install-plugin": "node esbuild.config.mjs production && node scripts/install.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.6.0"
  }
}
